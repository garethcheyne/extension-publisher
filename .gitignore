node_modules/
*.zip
.env*
!.env.store.example

# Credentials never belong in git. Google names a service account key
# <project>-<hex>.json, so ignore all JSON and list the files that are allowed.
*.json
!.claude-plugin/*.json
!templates/extension/*.json
*.pem
*.p12
